
// functions
// 1. What is wrong with this implementation? Move the cursor over the errors in the editor
// 2. Pass the correct types
// 3. is there something wrong with the type of "result"? Fix it.

const sum = (n1: number, n2: number): number => {
    return n1 + n2;
}

const result: boolean = sum("2", 2);